# Rà soát và phát hành 28/08/2026

## Chuẩn dữ liệu

- Đối chiếu bản nguồn Firestore: 166 bản ghi đơn vị, 330 liên hệ; GIS gốc `map34.json` có 124 ranh giới.
- Nhập thử vào database riêng, được khôi phục từ bản sao lưu production; không chạy test tạo SOS/phản ánh vào production.
- Kết quả chuẩn: 124 địa bàn, 124 ranh giới, 136 đơn vị, 126 trụ sở, 296 liên hệ (276 theo địa bàn + 20 cấp đơn vị), 4 nhóm / 34 hotline.
- Snapshot SHA-256: `8025a2edb8d8db3a906903a1b4c5d0fb8200e414ccfb9a7371a949c781969929`.
- Đối chiếu API cho từng địa bàn: tên, dân số, diện tích, mật độ, địa chỉ, mọi liên hệ công khai, tìm kiếm, tọa độ điểm nằm trong ranh; 124/124 đạt. Danh bạ bổ sung và hotline khớp nguồn.
- Import có transaction, xác nhận checksum/counts, kiểm tra hình học hợp lệ, idempotent. Fixture cũ được chuyển internal, không xóa tài khoản/hồ sơ hoặc đổi phân công.
- Snapshot chứa dữ liệu nguồn chỉ lưu riêng có kiểm soát quyền; không đưa vào Git hoặc thư mục public.

## Các lỗi đã vá

- Danh bạ SQL bị `LIMIT 1` và thay tên nguồn bằng tên chung: trả đủ thông tin được đánh dấu công khai.
- Địa bàn bị trở về Xuân Hương khi tải lại: lưu mã địa bàn trong URL; nhãn cảnh báo/tài khoản theo địa bàn đang xem.
- Bổ sung danh bạ 12 phòng nghiệp vụ/đồn KCN bằng nhóm thu gọn, dùng lại hệ giao diện đỏ hiện có.
- Cảnh báo trên bản đồ lấy từ API công khai đang hiệu lực; không thay bằng cảnh báo mẫu, không bịa tọa độ.
- Cổng cán bộ: không tải lại chi tiết cũ rồi xóa ghi chú mới sau một lần chuyển trạng thái thành công.
- Giữ các bản vá worker bản đồ, UUID trên HTTP, error boundary chống màn hình trắng và SOS không gọi nhanh.
- HTTPS cho IP, cookie Secure/HttpOnly, kiểm tra gia hạn mỗi 6 giờ, giới hạn upload nginx 32 MiB để nhận video 20 MiB sau base64.

## Bằng chứng kiểm thử trước phát hành

- Typecheck toàn workspace; 25 unit test; build production và kiểm tra worker MapLibre.
- 115 kiểm tra màn hình/điều hướng/lỗi chunk, khách + người dân + cán bộ, 375/762/1440 px, HTTP không secure-context và loopback secure-context. Sau vá race cán bộ chạy lại kiểm tra luồng và ma trận cán bộ.
- Luồng trình duyệt hai vai trò trên PostgreSQL riêng: tour 6 bước, đăng nhập, phản ánh có ảnh, SOS giữ 3 giây, quản lý điểm bản đồ, tiếp nhận → phân công → xác minh → xử lý → kết quả; người dân đánh giá và cán bộ xem điểm. Không tràn ngang; không lỗi JS.
- Ba địa bàn Xuân Hương, Tân Hà Lâm Hà, Bắc Gia Nghĩa: tìm kiếm, ranh giới, reload/deep-link, danh bạ mở rộng; mobile dọc/ngang và desktop.
- Cảnh báo cán bộ phát hành xuất hiện trong danh sách và popup bản đồ; worker JS và vector tile tải thành công.
- Tuần tra start/pause/resume/checkin/complete; báo cáo cuối ca; thống kê ngày/tháng/năm.
- Gia hạn thử chứng chỉ IP thành công trước bật chuyển hướng HTTPS.

Lệnh QA: `web/qa/release-regression.py`, `web/qa/data-parity.py`, `web/qa/full-data-browser.py`, `e2e/final_dual_role_workflow.cjs`. Các bài test có ghi dữ liệu bị chặn nếu không phải loopback/DB đánh dấu release-validation. Ảnh QA ở `tmp/release-full-data-extra`, `tmp/release-final`, `e2e/artifacts/final-workflow` (không commit).

## Giới hạn cần minh bạch

- KCN Tân Rai / Nhân Cơ chỉ có nguồn liên hệ để dựng đơn vị, chưa có địa chỉ/tọa độ đã xác minh.
- Nhiều trụ sở chưa có tọa độ chính thức; hiển thị địa chỉ và ranh giới, không tạo pin giả cho các địa bàn khác.
- Ranh 5 phường cũ và điểm CSKV minh họa tại Xuân Hương vẫn gắn nhãn tham chiếu/demo, không dùng để phân công nghiệp vụ chính thức.
- AI, VNeID quốc gia, điều phối 112/113, SMS/Web Push, camera và đồng bộ offline chưa tích hợp. Không tuyên bố đây là kênh cứu hộ chính thức.
- Cán bộ trực tiếp xử lý theo quyền pilot Xuân Hương; không mở rộng quyền hoặc tạo tài khoản trực ban giả cho 123 địa bàn còn lại.
- GPS thiết bị thật còn phụ thuộc quyền người dùng, cài đặt thiết bị và độ chính xác tín hiệu. QA dùng tọa độ trình duyệt kiểm soát được.

## Triển khai và khôi phục

Trước cập nhật: lưu dump PostgreSQL, web, API dist, cấu hình nginx/service/env vào thư mục root-only theo thời gian tại `/opt/dataforlife/backups/release-*`. Kiểm tra SHA từng asset bằng manifest bản đã QA. Giữ các chunk cũ để phiên đang mở không bị lỗi lazy-load.

Khôi phục web/API/config từ đúng bản backup nếu cần; **không tự phục hồi toàn DB lên trên dữ liệu người dùng mới**. Import chỉ upsert dữ liệu chuẩn và ẩn fixture; nếu cần đảo dữ liệu phải đối chiếu các bản ghi phát sinh sau release. Không đụng chứng chỉ/dịch vụ ngoài DataForLife.
